## Charmster

AI-powered private coaching app to practice love and build conversational confidence, with avatar-based practice partners (looks + voice previews loaded from Supabase Storage).

### Current state
- Onboarding hero now leads with a horizontal brand lockup (flaming heart + "Charmster" wordmark) loaded remotely from the public Supabase `App logo` bucket, sitting cleanly on the light gradient background.

### Integrations
- Supabase configured (auth + DB + Storage). Brand/avatar/voice assets stream from public buckets with mock-safe fallbacks.